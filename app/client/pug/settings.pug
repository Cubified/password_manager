doctype html
html
	head
		title Account Settings
		meta(name='viewport' content='width=device-width, initial-scale=1')
		link(rel='stylesheet' href='https://opensource.keycdn.com/fontawesome/4.7.0/font-awesome.min.css')
		link(rel='stylesheet' href='/css/bulma.css')
		style.
			.navbar,.navbar-brand{
				background:#ffdd57;
			}
			.navbar-burger.is-active > span{
				background:#333333;
			}
			a.navbar-item:hover,.navbar-burger:hover{
				background:#eecc47;
			}
	body
		nav(class='navbar')
			div(class='navbar-brand')
				a(class='navbar-item' href='/')
					strong
						h2(class='subtitle') passman
				div(class='navbar-burger')
					span
					span
					span
			div(class='navbar-end')
				div(class='navbar-menu')
					a(class='navbar-item is-info' href='/settings')
						h1(class='subtitle') Account Settings
					a(class='navbar-item is-danger' href='/logout')
						h1(class='subtitle') Logout
		div(class='modal' id='deleteModal')
			div(class='modal-background')
			div(class='modal-card')
				header(class='modal-card-head')
					p(class='modal-card-title') Delete Account?
				section(class='modal-card-body').
					This action <strong>cannot</strong> be undone, and all of your account data will be lost.
					<br><br>
					In order to continue, please enter your current password:
					<br><br>
					<input type='password' class='input is-danger' placeholder='Password' id='verifyPassText'>
					<br>
					<button class='button is-danger is-fullwidth' id='verifyPass'>Verify</button>
				footer(class='modal-card-foot')
					button(class='button is-danger' id='deleteAction' disabled) I Understand, Delete My Account
					button(class='button' id='deleteCancel') Cancel
		section(class='hero is-fullheight is-light')
			div(class='hero-body')
				div(class='container')
					div(class='field')
						label(class='label') Username
						input(type='text' class='input' placeholder='Username' id='username-change' value=username)
					button(class='button is-info is-outlined' id='update-username') Change Username
					br
					br
					div(class='field')
						label(class='label') Current Password
						input(type='password' class='input' placeholder='Current Password' id='current-password')
					div(class='field')
						label(class='label') New Password
						input(type='password' class='input' placeholder='Password' id='password-change')
					button(class='button is-info is-outlined' id='update-info') Change Password
					br
					br
					div(class='field')
						label(class='label') Two-Factor Authentication (Strongly recommended)
						button(class='button') Coming soon (or something like that)
					br
					br
					div(class='field')
						label(class='label') Beta UI
						label(class='checkbox')
							input(type='checkbox' id='beta-enable')
							span &nbsp; Enable
					hr
					a(class='button is-danger is-outlined' href='#' id='delete') Delete Account
		script(src='/js/settings.min.js')
